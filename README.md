# esp32-weather-dashboard
A live weather dashboard hosted on an ESP32. Connects to WiFi, fetches real-time weather data from OpenWeatherMap API, and serves a clean web interface accessible from any browser on the same network. Built with Arduino C++, HTTPClient, ArduinoJson, and WebServer libraries.
